machine:
  node:
    version: 6.3.0

general:
  branches:
    ignore:
      - gh-pages

deployment:
  production:
    branch: master
    commands:
      - git config --global user.email "infra@beta.gouv.fr"
      - git config --global user.name "CircleCI"
      - npm run deploy
